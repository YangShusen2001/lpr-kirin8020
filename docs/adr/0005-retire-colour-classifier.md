# ADR-0005 · 废弃颜色分类器；标签表确为旋转，且模型对绿牌本身不可靠

- **状态**：已接受（本 ADR 已更正一次，见「更正记录」）
- **日期**：2026-09-20
- **前置**：ADR-0001（继承资产）
- **证据**：本 ADR 的实测（`_scratch/settle_cls.py`，用项目**自己的**预处理）；`lpr-showcase/assets/js/pipeline.js:35,489`；`lpr-harmony/.../Index.ets:282-295`；`lpr-showcase/docs/adr/ADR-015`（原为"只登记不裁决"）

## 问题

`lpr-showcase` 遗留一个**未裁决的口径冲突**：颜色分类模型（`litemodel_cls_96x_r1`，1.53 MB，占单帧 2.69 ms / 7.4%）对 `hlpr-test.jpg` 判蓝牌，而牌面像素测量判真绿牌。原项目选择"只登记不裁决"。

该分类器应保留、修复，还是废弃？

## 决策

**废弃。颜色判定改用像素测量（绿色占比 + 色相），零模型开销。**

两条独立理由，**都成立，且是不同的问题**：

### 理由一：项目的标签表是错的（旋转）

源码 `pipeline.js:35` 写 `PLATE_COLOURS = ['yellow', 'blue', 'green']`，`Index.ets:293-295` 镜像为 `0→黄牌, 1→蓝牌, else→绿牌`。

用**项目自己的预处理**（`encodeClassify`：BGR 三平面 ÷255，非 RGB、非 ImageNet 归一化）实测四个**肉眼已核实**的样本：

| 样本 | 肉眼真值 | argmax | 输出 |
|---|---|---|---|
| `crop-0-津B6H920.jpg` | 蓝 | **0** | `0.999946 / 0.000028 / 0.000026` |
| `crop-1-皖KD01833.jpg` | 绿 | **1** | `0.000060 / 0.999850 / 0.000090` |
| `crop-8-冀D5L690.jpg` | 黄 | **2** | `0.000022 / 0.000509 / 0.999469` |
| 1000 张真实集（像素测量判 891 蓝） | 蓝为主 | **0 占 982/1000** | — |

→ **正确顺序是 `['blue', 'green', 'yellow']`。项目表是它的一个旋转。** 按项目表解码，五个已验证样本全部错标。

这解释了原来的"冲突"：分类器对 `hlpr-test.jpg` 输出 index 1 被解码成"蓝牌"，而 index 1 其实是**绿牌**。

### 理由二：即使标签表修正，模型对绿牌仍不可靠

用 7 张 CCPD2020 绿牌（8 字符，像素测量绿占比 0.967–0.995）实测：

```
argmax 直方图 = {0: 4, 1: 3}
```

**只有 3/7 落在正确的 index 1。** 且 `hlpr-test.jpg` 即使按修正表也判 index 0（蓝），置信度 0.99992。

→ 标签表修正后，模型在**蓝/黄**上可用，但在**绿牌**上准确率约 43%（7 张样本），不足以支撑"多牌型"主张。

## 理由（为什么仍要废弃而非修正）

1. **修正标签表只需改两行，但模型对绿牌的弱点是结构性的**，不是查表问题。
2. **像素测量已能独立判色**：`plate_face_colour.py` 的口径（绿占比 + 色相）在 1000 张上与修正后的分类器一致率约 96.5%，且对 7 张绿牌全部正确。
3. **删掉省 2.69 ms/帧（7.4%）**，像素测量约 0.1 ms。
4. **保留一个已知在关键类别上失效的组件会污染延迟预算**——为"展示分歧"而保留等于花 7.4% 帧预算买一个负面结果。

## 后果

- 生产流水线从四段变三段：检测 → 透视矫正 → 识别 →（像素判色）。
- `litemodel_cls_96x_r1.onnx` 移出生产路径，仅作论文对照物保留。
- 论文须给出：① 标签表旋转的证据（四样本 + 直方图）；② 修正后绿牌准确率仅 3/7；③ 像素测量作为替代口径的验证。
- `lpr-showcase` 的 `pipeline.js:35` 与 `Index.ets:293-295` 的**错误标签表须记录在案**，不得沿用。

## 更正记录

**2026-09-20 首次更正**。初版本 ADR 采信了一个中间结论，称"分类器基本是反的/随机"，并引用了"一致率 1.3%"与"16 种预处理配置最好只有 37.9%"。**该结论与那些数字都是用错误的预处理（RGB / ImageNet 归一化）算出来的**，不是项目实际使用的口径。

用项目自己的 `encodeClassify`（BGR ÷255）重跑后，真实情况是：**标签表旋转（理由一）+ 模型对绿牌不可靠（理由二）**，而非"模型整体失效"。废弃决策不变，但证据与解释已改写。

教训：**复现一个组件的输出前，必须先读它自己的预处理源码**；否则测的是自己的假设，不是被测对象。

## 边界声明

- 绿牌样本 n=7，**统计上很弱**。"约 43%"只作方向性陈述，不得当作精确准确率引用。
- 像素测量的阈值标定自 1000 张真实集与 7 张 CCPD 绿牌，**极端欠曝/强反光未测**。
- `hlpr-test.jpg` 的真值仍有争议：ADR-015 据像素+几何推定为 8 字符绿牌，而分类器（修正表后）判蓝。本 ADR **不裁决该样本**，只裁决"分类器不可作为判色依据"。
- 牌色**不进入**识别主链：车牌串由识别器产出，牌色是附带属性。置信度低时应输出"未知"。

## 未完成动作

1. 把 `plate_face_colour.py` 的判据移植为 C++/NAPI（生产路径不得依赖 Python）。
2. 在更大绿牌样本上重测分类器，把"约 43%"换成有区间的数字。
3. 在极端光照样本上标定像素判色的失效边界。
