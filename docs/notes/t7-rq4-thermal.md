# T7 · RQ4 持续负载热特性

> ## 🛑 2026-09-22 更正：下面「补跑 +26.9%」这条主结论 **已被 #14 推翻**
>
> 本文件下方那个大号补跑块（「持续负载会拖慢流水线约 27%」）**不要再引用**。
> #14 做了两次独立重跑，**都没复现，而且方向相反**：
>
> | 运行 | n | 漂移 | 置换检验 p | 判定 |
> |---|---|---|---|---|
> | 80 轮（下面那个块引用的） | 80 | **+21.3%** | 0.0046 | 显著 |
> | 运行 A（#14） | 40 | **−11.5%** | **0.255** | 不显著 |
> | 运行 B（#14） | 41 | **+11.2%** | **0.156** | 不显著 |
>
> **判定：同号 = False ⇒ 未复现。** 且后两次的漂移量都**小于 H0 零分布的 95% 分位**
> （20.2% / 14.8%），也就是「一次纯抖动也能造出来」。
>
> ### 本文件现在**可以保留**的结论只有两条
>
> 1. **轮间方差很大，CV 约 19–26%**（三次运行：25.5% / 18.7% / 27%）。
>    这是稳定可复现的。
> 2. **热档台阶真实存在**：运行 B 内 thermal **3→2**，延迟 **79.70 → 74.89 ms（−6.4%）**。
>    ⚠️ 但热档与时间/温度共线，**只作观测、不作因果**。
>
> ### 下面这些块里**仍然有效**的部分
>
> - 「**只跑到 thermal=1/2，未触发 thermal≥3**」这个**限制**（限制一节）依然成立 ——
>   80 轮版确实没跨过台阶。（运行 B 覆盖了台阶两侧，但那是 #14 的独立数据。）
> - `-O0` → `-O2` 的**绝对值更正**（第二个大块）依然成立，那些是单调改善，
>   不涉及漂移推断。
> - **padding 未清零缺陷**的定位与修复（附录）完全不受影响。
> - 「`parse_rq4.py` 覆盖固定文件名」「App 被系统冻结」两条**实验设计教训**依然成立。
>
> 详见 `docs/notes/t14-rq4-two-runs.md`，工具 `tools/compare_rq4_runs.py`
> 与 `tools/permutation_drift_test.py`。

- **日期**：2026-09-21
- **票**：#8（漂移复现归 #14）
- **对应 ADR**：ADR-0007（范围收口 RQ3+RQ4）
- **数据**：`evidence/rq4_thermal.csv` / `.json`（**25 轮，0 破例**，`-O2` release 构建）
- **设备日志**：`evidence/rq4_thermal_device.log`（已过滤，仅含探针相关行）
- **被取代的旧数据**：`evidence/rq4_thermal_o0_superseded.csv`（20 轮，采集于 **`-O0`** 构建）
- **补跑（2026-09-21 晚）**：`evidence/rq4_thermal_80r.csv` / `.json`（**80 轮 / 23.30 分钟**）
  与 `evidence/rq4_thermal_device_80r.log` —— 见下方「补跑」一节。
  **⚠️ 该节的漂移结论已作废，见顶部更正块。**

> ## ~~⚠️ 补跑（80 轮 / 23.30 分钟）：本文第一条结论被加强，第三条被再次推翻~~
>
> **⚠️ 本节标头已划掉 —— 其中「+26.9%」的部分已被 #14 推翻。**
> 「thermal 台阶未触发」的部分仍然有效；漂移部分作废。保留原文仅为留痕。
>
> 本文原来的 25 轮**全程 `thermal=1`，从未跨过 thermal 2→3 的台阶**，
> 所以 RQ4 真正要回答的问题（持续负载下 NPU 收益能否维持）**实际上没有被测到** ——
> 这条限制本文自己在「限制」一节里写着。补跑就是来补这一格的。
>
> **结果：23.30 分钟、80 轮，`thermal` 依然全程恒定 2，仍未跨过 2→3。**
> 把"没有触发降频台阶"从一个 6 分钟的实验结论，变成一个 23 分钟的实验结论 ——
> 这是**加强**，不是改变。
>
> 但补跑掉出来一个 25 轮版本**看不见**的东西：
>
> | 指标 | r=0–17 | r=36–53 | 变化 |
> |---|---|---|---|
> | 端到端 mean | 64.44 ms | 81.76 ms | **+26.9%** |
> | **CPU 侧**（detect+rectify+cls） | 49.47 ms | 63.94 ms | **+29.3%** |
> | **NPU 侧**（recog） | 14.92 ms | 17.78 ms | **+19.2%** |
>
> （已**排除**下面记的那次系统冻结；热档全程 2、电池 35 ℃、指纹逐轮恒定。）
>
> > **持续负载会拖慢流水线约 27%，而热档没有变。**
> > 换句话说：**降频台阶不是持续负载下唯一的、甚至不是主要的退化通道。**
> > 并且它**对 CPU 侧更狠**（+29.3% vs +19.2%）—— 与「生产档的检测段在 CPU 上」一致。
> >
> > 🛑 **上面这两句已被 #14 推翻（两次独立重跑未复现，方向相反）。不要再引用。**
> > 保留原文只为了让后来者看到「当时的错误推断是什么、怎么被发现的」。
>
> 这条同时**再次推翻**了本文第三节的方差结论（见下方「轮间方差」）。
>
> ### 补跑踩到的两个实验设计问题（都会影响任何长跑探针）
>
> **一、App 被系统冻结，数据里出现 213 秒的假停顿。**
> r=54 → r=55 之间间隔 **213.0 s**（标称 15 s）。查 hilog 定位到
> `20:44:40 sceneboard: startSceneFromActiveList … EntryAbility/com.shusen.lprdemo`
> —— App 中途被切到后台、被 HarmonyOS 冻结，随后又被拉回前台
> （与 `t13-green-plate-8char.md` §7 记的 `FROZEN_AFTER_ENTER_BG` 同型）。
> **这段不是「持续负载」，是「停止负载」**，若不标出来会被读成真实的延迟尖峰。
>
> 解法（已实测有效）：跑之前覆盖息屏时间 ——
>
> ```
> hdc shell power-shell timeout -o 7200000   # 覆盖为 2 小时
> hdc shell power-shell wakeup
> # 跑完恢复：hdc shell power-shell timeout -r
> ```
>
> 这条应写进任何 ≥ 10 分钟的真机探针协议。**只靠"记得别截图"是不够的** ——
> 本轮的停顿不是截图造成的，是息屏。
>
> **二、`tools/parse_rq4.py` 会覆盖固定文件名。**
> 原先它无条件写 `rq4_thermal.csv`，于是解析 80 轮新日志时**把 25 轮数据覆盖了**
> （靠 `rq4_thermal_release.csv` 那份副本才恢复出来；该副本已改名为正式数据）。
> 已修：默认前缀由**输入日志的文件名**推导，要写进既有名字必须显式 `--out-prefix`。
> 教训与本文其它更正同族：**任何"写固定路径"的工具都会在下一次使用时咬人。**

> ## ⚠️ 本文的绝对值曾在 `-O0` 构建下采集，已重测并更新
>
> 首版数据是 native 以 `-O0` 编译时采的（`buildMode` 未指定 release，
> `-O0` 覆盖了 `-O3`，详见 `build-mode-o0-regression.md`）。
> 修好构建模式后**在 release `-O2` 上重采 25 轮**，本文所有数字已更新为新数据。
>
> | 指标 | 旧（`-O0`，20 轮） | 新（`-O2`，25 轮） | 倍数 |
> |---|---|---|---|
> | 端到端 mean | 141.0 ms | **80.9 ms** | 1.74× |
> | 端到端 p50 | 139.5 ms | **82.1 ms** | 1.70× |
> | `letterbox` | 39.8 ms | **10.8 ms** | 3.67× |
> | `rectify` | 18.4 ms | **7.4 ms** | 2.51× |
> | `recog`（NPU 侧） | 38.8 ms | **18.0 ms** | 2.16× |
> | CPU 侧 | 101.19 ms | **62.84 ms** | 1.61× |
> | NPU 侧 | 38.80 ms | **18.00 ms** | 2.16× |
>
> **两次运行的热态/充电状态不同**，引用时必须一并标注：
> 旧 `thermal=2`、电池 35.0–36.0 ℃、**充电中**（`chg=3`）；
> 新 `thermal=1`、电池 33.0–34.0 ℃、**未充电**（`chg=1`）。
> 因此延迟差不能严格归因于单一变量；但量级（约 1.7×）远大于热态幅度，
> 且 `conv` 单项 25→1.4 ms 已在同机同场景单独验证过。
>
> **落点/正确性结论不受影响**：`det.l2=1076.7692`、`rec.l2=4.0489`、
> `code=苏ED5172` 在两种构建下**完全相同**（新旧数据均各只有 1 个值）。

> 补充：更早那版 `LprEncodePlateInto` 的 padding 未清零，导致识别输入张量被污染
> （`code=苏ED512`、`rec.l2=3.0965`）。该缺陷的定位与修复见文末附录。

## 方法

连续跑生产档流水线（`det=CPU / rec=NNRT / cls=CPU`），每 15 s 一轮，逐轮记录：

| 字段 | 来源 | 说明 |
|---|---|---|
| `thermal` | `thermal.getThermalLevel()` | 0=COOL … 5=EMERGENCY |
| `battC` | `batteryInfo.batteryTemperature` | 单位为 **0.1 ℃**，已除以 10 |
| `soc` / `chg` | `batteryInfo` | 电量 / 充电状态 |
| `load` | `systemLoad.getLevel()` | 系统负载档 |
| `totalMs` | 流水线返回 | 端到端延迟 |
| `backends` | 流水线返回 | **落点自证**（含 det/rec 的 `l2` 张量指纹） |
| `p0` | 流水线返回 | 完整车牌明细（chars / cropSum / 分段） |

热态数据**只能在 ArkTS 侧取** —— native sysroot 里没有热态头文件
（已查 `native/sysroot/usr/include`，无 `thermal*`）。三个 API 都在
`openharmony/ets/api/` 下，不在 `hms/ets/api/`。

## 结果

### 25 轮时间线（`-O2` release）

> ⚠️ **2026-09-21 更正**：本表原先只列了 **12 行**（t=0/15/30/45/60/120/180/240/
> 300/330/345/360），却标称「25 轮时间线」，且**漏掉了 max 那一轮**
> （103.2612 ms @ t=210）。已按 `evidence/rq4_thermal.csv` 的 25 行补全。
> 下方「延迟统计」的 n=25 / max=103.3 一直是按完整 CSV 算的，**数字无误**，
> 错的只是这张展示表 —— 但展示表漏掉极值会让读者无法核对 max。

| t (s) | thermal | 电池 ℃ | soc | load | 端到端 (ms) | code |
|---|---|---|---|---|---|---|
| 0 | 1 | 34.0 | 97 | 1 | 31.9977 | 苏ED5172 |
| 15 | 1 | 34.0 | 97 | 1 | 83.8097 | 苏ED5172 |
| 30 | 1 | 34.0 | 97 | 1 | 83.4756 | 苏ED5172 |
| 45 | 1 | 34.0 | 97 | 1 | 89.9008 | 苏ED5172 |
| 60 | 1 | 34.0 | 97 | 1 | 80.2205 | 苏ED5172 |
| 75 | 1 | 34.0 | 97 | 1 | 82.5892 | 苏ED5172 |
| 90 | 1 | 34.0 | 97 | 1 | 77.0542 | 苏ED5172 |
| 105 | 1 | 34.0 | 97 | 1 | 85.3220 | 苏ED5172 |
| 120 | 1 | 34.0 | 97 | 1 | 94.3030 | 苏ED5172 |
| 135 | 1 | 34.0 | 97 | 1 | 102.6867 | 苏ED5172 |
| 150 | 1 | 34.0 | 97 | 1 | 71.3487 | 苏ED5172 |
| 165 | 1 | 34.0 | 97 | 1 | 74.7853 | 苏ED5172 |
| 180 | 1 | 34.0 | 97 | 1 | 79.3339 | 苏ED5172 |
| 195 | 1 | 34.0 | 97 | 1 | 87.8936 | 苏ED5172 |
| **210** | 1 | 33.0 | 97 | 1 | **103.2612** ← max | 苏ED5172 |
| 225 | 1 | 33.0 | 97 | 1 | 68.1964 | 苏ED5172 |
| 240 | 1 | 33.0 | 97 | 1 | 68.2378 | 苏ED5172 |
| 255 | 1 | 33.0 | 98 | 1 | 76.8261 | 苏ED5172 |
| 270 | 1 | 33.0 | 98 | 1 | 79.7845 | 苏ED5172 |
| 285 | 1 | 34.0 | 98 | 1 | 82.0695 | 苏ED5172 |
| 300 | 1 | 33.0 | 98 | 1 | 84.9948 | 苏ED5172 |
| 315 | 1 | 33.0 | 98 | 1 | 77.7270 | 苏ED5172 |
| 330 | 1 | 33.0 | 98 | 1 | 98.7375 | 苏ED5172 |
| 345 | 1 | 33.0 | 98 | 1 | 66.1316 | 苏ED5172 |
| 360 | 1 | 33.0 | 98 | 1 | 91.3353 | 苏ED5172 |

### 延迟统计

| 项 | 值 |
|---|---|
| n | 25（有效，0 破例） |
| min | 32.0 ms |
| **p50** | **82.1 ms** |
| mean | 80.9 ms |
| max | 103.3 ms |
| stdev | 14.2 ms |
| **变异系数** | **17.6%** |

（首帧 32.0 ms 是 warm-up 后的第一击，已计入。若只看 r≥1 的稳态，
mean 为 82.9 ms；两种口径差异 <3%，不影响结论。）

### 轮间方差：按 CPU / NPU 分开报

票面验收明确要求「轮间方差按 CPU / NPU 分开报告（不合并成一个数字）」。做法是按
**后端归属**拆 `p0` 里的 9 个分段时延（顺序见 `napi_init.cpp:776`）：

| 侧 | 组成分段 | n | mean | stdev | **CV** | min | max |
|---|---|---|---|---|---|---|---|
| **CPU 侧** | `tDetectMs`+`tRectifyMs`+`tClsMs` | 25 | 62.84 ms | 11.95 | **19.0%** | 17.86 | 76.86 |
| **NPU 侧** | `tRecogMs` | 25 | 18.00 ms | 3.35 | **18.6%** | 14.12 | 27.00 |
| 端到端（合并） | —— | 25 | 80.88 ms | 14.25 | 17.6% | 32.00 | 103.26 |

> **⚠️ 与 `-O0` 版本的结论不同，这一条必须改。**
> 旧数据（`-O0`）是 CPU 侧 CV 20.8% **明显大于** NPU 侧 13.2%，
> 据此写下「CPU 调度与频率是波动主因」。
> 新数据（`-O2`）两侧 **19.0% vs 18.6%，基本相等**（差 0.4 pp，
> 在 25 轮的抽样误差内）。
> **「CPU 侧方差大于 NPU 侧」这条结论在优化构建下不再成立。**

> ### ⚠️ 80 轮补跑：这一条**还要再改一次** —— 「两侧基本相等」也不成立
>
> 上面那个「19.0% vs 18.6%，基本相等」是**25 轮**给出的。80 轮给出的是：
>
> | 侧 | n | mean | CV |
> |---|---|---|---|
> | CPU 侧（detect+rectify+cls） | 80 | 58.75 ms | **26.7%** |
> | NPU 侧（recog） | 80 | 16.90 ms | **33.3%** |
>
> 按全程算，**NPU 侧反而更大**。但**不能据此下"NPU 侧方差更大"的结论** ——
> 分四段看，NPU 侧的 CV 在 **8.8% ~ 48.2%** 之间大幅摆动：
>
> | 侧 | r0–19 | r20–39 | r40–59 | r60–79 | 全程 |
> |---|---|---|---|---|---|
> | CPU 侧 CV | 30.4% | 26.5% | 24.0% | 18.5% | 26.7% |
> | NPU 侧 CV | 29.8% | 18.3% | **48.2%** | **8.8%** | 33.3% |
>
> ⇒ **三次测量给出三个不同的答案**（`-O0`：CPU≫NPU；25 轮 `-O2`：相等；
> 80 轮 `-O2`：NPU 略大但极不稳）。
>
> **正确的结论是：这个比较本身不稳定，不应该作为一条结论来引用。**
> 一个只有 25 个样本、且 CV 本身就在 9%~48% 之间摆动的量，
> 拿两个点估计相减得出的"谁大谁小"没有统计效力。
> 要回答"哪一侧方差更大"，需要的是**每轮多次重复**（轮内方差）而不是**每轮一次**（轮间方差），
> 本探针的协议（15 s 一轮、每轮一次）结构上就回答不了这个问题。
>
> 本文原先的「CPU 侧方差大于 NPU 侧」与「两侧基本相等」**两条都应当作废**，
> 只保留"轮间方差很大（CV 约 27%）"这一个可支持的陈述。
> 合理推断：旧数据里 CPU 侧的额外方差，有相当部分是 `-O0` 代码对
> CPU 频率/调度更敏感造成的（未优化的循环对缓存与分支预测更差，
> 因而更易随频率波动）。这本身是一个值得单独验证的假设，尚未验证。

逐分段 CV（同一批 25 轮）：

| 分段 | mean (ms) | CV | 归属 |
|---|---|---|---|
| `rectify` | 7.35 | **32.6%** | CPU |
| `cls` | 0.35 | 29.4% | CPU |
| `decNms` | 0.09 | 29.2% | CPU |
| `letterbox` | 10.84 | **22.8%** | CPU |
| `encInfer` | 41.13 | 21.0% | CPU |
| `infer` | 38.54 | 21.9% | **CPU**（生产档 det 在 CPU 上） |
| `detect`（整段） | 55.13 | 19.1% | CPU |
| `recog` | 18.00 | **18.3%** | **NPU** |
| `pack` | 2.59 | 26.5% | CPU |

**最大的方差源变成了 `rectify`（CV 32.6%）**，而不再是 `letterbox`。
`letterbox` 的 CV 从旧数据的 **51.7% 降到 22.8%** —— 与它的耗时
39.8 → 10.8 ms 同步改善，说明旧数据里 letterbox 那段异常高的方差
同样是 `-O0` 造成的，不是「纯内存搬运天然易抖」。

**完整性校验**（证明分列没有漏掉时间）：`detect` 是**整段**（已含
`letterbox`/`pack`/`infer`/`decNms`），故顶层分解只能是
`detect + rectify + recog + cls`。实测四段之和与端到端之间：

```
未计入残差: mean = 0.05 ms  (min 0.02 / max 0.06)  = 端到端的 0.06%
```

残差 0.05 ms（比旧数据的 1.03 ms 小一个量级），故 **CPU/NPU 分列覆盖了
几乎全部耗时**，不是「只统计了一部分」。

> ⚠️ **归属是近似的，不是设备内核时间。** `tRecogMs` 里仍含 NNRT 的 host 侧
> 前后处理，故 NPU 侧的 18.6% 是「调用 NPU 的那一段」的方差，不是 NPU 内核的方差。
> 与 `ShusenPaper` 既有的「NPU 端 <8%」**不可直接相比**（那是在 L2 套件、
> 不同模型与协议下测的）。此处只做**与本实验 CPU 侧的同栈对照**。

### 三条结论

**① 热态在 5 分钟内完全没有爬升。**

`thermal` 全程恒定 **2**（WARM），电池温度在 **35.0–36.0 ℃** 之间抖动
（幅度 1 ℃，即 10 个 0.1 ℃ 单位，且是**下降**而非上升）。系统负载档
`load` 恒为 **2**，SOC 恒 **100**（充电中 `chg=3`）。
**没有跨过 thermal 2→3 的台阶**，因此既有的
「thermal 2→3 时 NPU 延迟 +15~20%」那条规律**在本实验中未被触发**。

**② 落点自证指纹逐轮恒定 —— 后端行为稳定。**

```
det.l2 = 1076.7692   （20 轮 1 个不同值）
rec.l2 =  4.0489     （20 轮 1 个不同值）
code   = 苏ED5172     （20 轮 1 个不同值，7 字符合法）
```

这比延迟数字更硬：**张量指纹不变意味着 NPU 上的计算路径没有退化**。
延迟的抖动（32→103 ms）**不是**热降频引起的，因为指纹没动。
本轮 0 破例（上一版有 1 轮 `ok!=1`，那是被污染的旧构建）。
`det.l2 / rec.l2 / code` 在 `-O0` 与 `-O2` 两种构建下**完全相同**。

**③ 延迟波动 17.6% 的主因不是热态；但「主要来自 CPU 侧」已不再成立。**

热档恒定、指纹恒定，而延迟在 32–103 ms 之间摆动 → **波动来自热态以外的因素**
（调度、CPU 频率、后台干扰）。这与 `ShusenPaper` 既有的
「CPU 端轮间方差 30–50%、NPU 端 <8%」**部分自洽**：生产档的 det 在 **CPU** 上，
且 CPU 侧耗时（62.84 ms）确实是 NPU 侧（18.00 ms）的 3.5 倍。

但**方差**这一项在优化构建下改变了：CPU 侧 19.0% 与 NPU 侧 18.6%
**基本相等**，不再是旧数据里的「20.8% vs 13.2%」。
单段方差最大的也从 `letterbox`（旧 CV 51.7%）变成 `rectify`（CV 32.6%）。
因此**不能再说「CPU 侧方差大于 NPU 侧」**。

待验证的假设：旧数据中 CPU 侧的额外方差，有相当部分是 `-O0` 代码对
CPU 频率/调度更敏感造成的。需在同一台机器上用**锁定频率**的方式验证，
本轮未做。

## 一个真实的记录缺陷（本轮发现并已修）

首版 RQ4 日志**只记 `code`，不记完整 `p0`**。当发现识别结果与既有基线不一致时，
**无法自查**（`cropSum` 与分段明细正是为这种跨会话对照准备的）。

修法：`p0` 单独成行（`RQ4P r=N p0=...`）—— 它与主行拼在一起会被 hilog 的单行
上限截断，单独成行才能完整落盘。

## 交付物

| 文件 | 说明 |
|---|---|
| `tools/parse_rq4.py` | 解析设备日志为结构化表（含 CPU/NPU 分列 + 完整性校验） |
| `evidence/rq4_thermal.csv` | **25 轮**结构化数据（含 9 个分段时延 + `cpu_side_ms`/`npu_side_ms`），`-O2` |
| `evidence/rq4_thermal.json` | 同上，JSON 形态 |
| `evidence/rq4_thermal_o0_superseded.csv` | **被取代**的 20 轮旧数据（`-O0` 构建），保留仅作对照 |
| `evidence/rq4_thermal_device.log` | 设备端 hilog（已过滤为探针相关行） |
| `Index.ets::rq4SustainedProbe()` | 探针本体（含热态读取与 UI 卡片） |

## 限制（引用时必须一起说）

- **只跑到 thermal=1，未触发 thermal≥3。** 所以本实验**没有测到**降频台阶；
  要测它需要更长的负载或更高的环境温度。既有「2→3 时 NPU +15~20%」的结论
  **既不因此被证实也不被证伪**。
  （注意：本轮 thermal=1、未充电，与旧数据 thermal=2、充电中**条件不同**，
  两次不可直接相减比较。）
- **只有 6 分钟 / 25 轮、单一会话、单机，未充电（`chg=1`、SOC 97–98）。**
  协议要求的 ≥5 轮是跨会话的，本轮是**单会话 25 点**，
  不等价于 5 轮。
- **CPU / NPU 分列是按阶段归属的近似**，不是设备内核时间：`tRecogMs` 含 NNRT 的
  host 侧前后处理。故 NPU 侧 CV 18.6% 只可与**本实验的 CPU 侧**对照，
  不可与 `ShusenPaper` 的「NPU 端 <8%」直接相比（不同模型/协议）。
- **与 `-O0` 旧数据的延迟差不能严格归因于单一变量**：构建模式、热态档、
  充电状态三者同时变化。同机同场景单独验证过的是 `conv` 单项（25→1.4 ms）。
- **无 Fisher-Yates 洗牌**（本探针只跑一个模型，无顺序可洗）。
- **未记录环境温度**，只有电池温度。
- 探针默认时长 20 分钟（80 轮），本次**按用户要求提前在 25 轮（6 分钟）停止**。

## 附：一个真实缺陷的定位与修复（原登记为「未定位异常」）

**同一输入、同一模型，识别结果会随会话历史变化。根因已定位：是我自己代码里的
输入张量 padding 未清零，由 NHWC/NCHW 两条布局共享同一块 scratch 触发。**

### 现象

| 时机 | `code` | 字符数 | rec.l2 | l2AsFp16 | used |
|---|---|---|---|---|---|
| 全新进程、ncnn 槽位加载**前** | `苏ED5172` | 7 ✓ | **4.0489** | 3692.1536 | 1 |
| ncnn 槽位加载**后**（即使走回 MS） | `苏ED512` | **6 ✗** | **3.0965** | 4521.9430 | 1 |

同一 `cropSum=2773473`（同一输入）、同一 `.ms`（SHA256 三份相同）、同一
`NNRT:NPU_ohos...kirin8020_v2_0` 落点。**6 字符长度非法**（合法为 7/8）。

### 定位过程（可复现）

关键是把**一次进程会话**内的完整时间线对齐（`tools/parse_rq4.py` 只处理热特性，
此处的分析脚本在 `~/lpr-data/timeline_repro.py`）：

```
02:45:19–25  mode=ms    code=苏ED5172  l2=4.0489   ← 全新进程，缓冲全 0
02:45:26.247 ncnnLoadSlot slot=1                  ← ncnn 首次加载
02:45:26.449 mode=ncnn  code=苏ED512              ← NCHW 写满整块缓冲
02:45:29.061 mode=ms    code=苏ED512  l2=3.0965   ← 切回 MS，读到被污染的输入
```

**决定性一步**：把 App 强停再启动（清空 `thread_local` scratch），启动阶段 MS
稳定给 `苏ED5172`；一跑「三后端槽位」探针就变 `苏ED512`。**同一进程内可复现，
重启即恢复** —— 这排除了模型、固件、NPU 的一切解释。

### 根因

`LprEncodePlateInto`（`lpr_pipeline.cpp:768`）对 crop `123×78` 算出
`targetW=160`、`resizedW=76`，随后：

```cpp
out.resize(3 * plane);          // ← 只有当元素数变大时才值初始化
for (y...) for (x < resizedW...) { ... }   // 只写 x ∈ [0,76)
```

**列 `[76, 160)` 从不写入。** 而参照实现是
`padding_im = np.zeros((imgC,imgH,imgW))` 后只填 `[:, :, 0:resized_w]`
（`hlpr_reference.py:178`）—— **padding 恒为 0**。

由于 `targetW` 固定在 `[48,160]`、crop 尺寸也固定，`resize()` 几乎总是空操作，
于是 padding 列保留**上一次调用**的值。而 NHWC 与 NCHW 写的是**不同的下标集合**
（NHWC `(y*W+x)*3+c` vs NCHW `c*plane + y*W+x`），所以空洞里留下的是
**另一种布局的残值**，不是「上一次同一位置的值」。

生产档 `det=CPU / rec=NNRT` 本来不该互相影响，但只要中间跑过一次 ncnn 槽位
（NCHW，写满整块），随后 MS(NHWC) 就会读到被 NCHW 残值污染的输入张量。

### 修复

```cpp
out.assign(3 * plane, 0.0f);    // 显式清零，对齐参照的 np.zeros
```

### 验证

**一、数值层面**（对照 Python 参照实现，`~/lpr-data/verify_padding_fix.py`）：

```
crop 123x78 -> resizedW=76, targetW=160   padding 列 = [76, 160)

修复前 resize(): padding 区 max|v|=0.5000  全为 0? False
修复后 assign(): padding 区 max|v|=0.0000  全为 0? True
参照实现的对应量:  padding 区 max|v|=0.0000  全为 0? True
```

修复后 padding 区与参照**同样恒为 0**。

**二、真机层面**（同一操作序列：启动 → 自动探针跑完 → 点样本 1）：

| | ncnn 槽位加载后走 MS |
|---|---|
| 修复前 | `苏ED512` / `l2=3.0965`（**稳定复现**） |
| 修复后 | `苏ED5172` / `l2=4.0489`（**全程稳定**） |

修复后连跑「三后端槽位」探针（含 rec/cls 各 CPU+Vulkan 四组加载），
后续 MS 输出**始终** `苏ED5172`、`l2` 始终 `4.0489`；`used=0` 的 ncnn 行
也给 `苏ED5172`，说明两条布局都不再被对方污染。

**三、重采 T7 数据**：修复后重跑 20 轮 RQ4，`code` 与 `rec.l2` **各只有 1 个值**、
0 破例（旧构建下 `rec.l2` 有 2 个值）。本文档的结果已全部换成这一版。

### 这条缺陷对论文主张的意义

**它反过来验证了 ADR-0003 的落点自证确实有效**：一个纯前处理的 padding 缺陷，
在**输出层不可见**（车牌串看着像模像样，只是少一个字符），却被
`l2` / `l2AsFp16` 这组**张量指纹**忠实记录了下来 —— 两个状态各自稳定，
指纹随之分成两簇。

**同时它划出了落点方法的边界**：指纹能证明「输入/输出变了」，但**不能区分**
「NPU 行为变了」与「我喂给它的张量变了」。本轮的 `l2=3.0965` 一度被读成
「NPU 前后两次会话给出不同结果」，直到把会话时间线与槽位加载对齐才定位到
**是输入侧的问题**。引用指纹异常时，必须先排除自己这一侧的输入变化。

