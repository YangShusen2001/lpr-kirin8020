# 麒麟 8020 端侧异构推理与车牌识别流水线

本上下文研究**国产工艺达芬奇 NPU（麒麟 8020）在 HarmonyOS 原生栈上的实际可用性**，并以中文车牌识别四级流水线作为应用级案例。它的存在理由是：学术界对麒麟 NPU 的系统评测止步于 2019 年的麒麟 990，而移动端 NPU 评测文献（MELTing Point 等）几乎不覆盖国产工艺。

## 语言

### 落点与证据

**落点（LANDED）**：
一次推理**实际**执行的硬件后端，由运行时日志字段或输出张量指纹判定，而非由请求参数推定。
_Avoid_: 后端、targetDevice、委托目标、命中硬件

**自证（self-evidencing）**：
落点判据必须能从原始日志或张量里**直接读出来**，不需要读者相信一句结论。反例：`LANDED=CPU` 分不清"我们选的"还是"静默回落的"。
_Avoid_: 验证、确认、证明

**回落（fallback）**：
请求了某个后端但实际落在另一个后端。必须与"选择"在日志里可区分（`req=` / `fallback=` 字段）。
_Avoid_: 降级、退化

**张量指纹（tensor fingerprint）**：
以输出张量的统计量（L2 范数、`L2asFp16` 重读值）作为"该后端确实算过"的证据，用于替代不可读的硬件利用率。
_Avoid_: checksum、校验和

**参照串（reference string）**：
参考实现自身的输出，用于对齐跨语言移植的保真度，**不构成任何正确性证据**。历史事故：`苏ED5172` 曾被当作样本真值用于验收，实为被测系统自己的输出。
_Avoid_: 真值、期望值、ground truth、标注串

**真值（ground truth）**：
人工标注的车牌字符串。本项目**只有** `_dataset/real/crops/` 的文件名是真值。
_Avoid_: 标签、参照、期望

**match=**：
「与同角色 CPU 基线逐字符一致」。用于捕捉**换后端引入的输出变化**，不构成正确性证据。字段名保留仅为历史日志可比。
_Avoid_: 正确、通过、对

### 算子与后端

**独立委托（standalone delegation）**：
算子单独构图时被 NNRT 接受并落 NPU。36 个被测算子中 20 个如此。
_Avoid_: 算子支持、算子兼容

**融合携带（fusion carry-over）**：
算子单独构图会被拒，但作为相邻 Conv 的**融合邻居**仍被带上 NPU。ResNet/MobileNet 内含大量 Relu/MaxPool/Sigmoid 却整体 8–13× 加速，即此机制。
_Avoid_: 算子支持、隐式支持

**固定委托开销地板（delegation floor）**：
NNRT 单次调用的固定开销下限（实测 ~0.3 ms），与算子规模无关。因此微算子落 NPU **必然**负收益。
_Avoid_: 启动开销、固定成本

**FULL_QUANT / WEIGHT_QUANT**：
两种 INT8 形态。前者（激活也量化）在麒麟 8020 上 **5/5 整图静默回退 CPU**；后者（仅权重）可留 NPU，checksum 误差 ≤0.25%。**8020 上唯一可用的 INT8 形态是 WEIGHT_QUANT。**
_Avoid_: INT8、量化（不加限定）

### 基准与协议

**L1 / L2 / L3 基准**：
算子级微基准 / 模型级套件 / 流水线级案例研究，三层。
_Avoid_: 微基准、端到端（单独使用）

**RQ1–RQ4**：
四个研究问题——表征（RQ1）/ 跨 SKU 扩展律（RQ2）/ 流水线异构放置（RQ3）/ 持续负载热特性（RQ4）。

**服务时间（service time）**：
单帧处理耗时。`1000/p50(frameMs)` 是它的理论倒数，**不是吞吐**。
_Avoid_: fps、帧率、吞吐（当指服务时间时）

**到达率 / 完成率**：
相机真实交付的帧率 / 流水线真实完成的帧率。**这是唯一可以对外称 fps 的量。**
_Avoid_: 出帧率、实际帧率

**热态（thermal level）**：
系统热档位。2→3 时 NPU 延迟 +15~20% 而 CPU <2%，因此**任何加速比必须标注热态**，否则不可比。
_Avoid_: 温度、发热

**轮间方差（inter-run variance）**：
同一构建、同协议、不同轮次之间的差异。实测 CPU 端 30–50%、NPU 端 <8%——因此**CPU 绝对延迟的单点数字不足以支撑结论**。
_Avoid_: 噪声、抖动

### 车牌领域

**多牌型**：
蓝牌 / 黄牌（单层、双层）/ 绿牌新能源（8 位）/ 白牌 / 黑牌 / 港澳两地牌 / 使馆 / 警用 / 军用 / 农用车等。**v1 只承诺蓝牌与绿牌。**
_Avoid_: 车牌类型、牌种

**省份偏斜（province skew）**：
评测集的省份分布与真实路面分布不一致，导致省份位错误被系统性低估。现有 1000 张真实集 **956/1000 是皖**，84 个同长度替换错误里 **78 个发生在省份位**。
_Avoid_: 数据不均衡、类别不平衡

**绿色占比（green fraction）**：
牌面像素中落在绿色色带的像素比例，用于**独立**判定牌色。与颜色分类器模型输出是两套口径，二者分歧时以像素测量为准。
_Avoid_: 牌色、颜色（不加限定）

**双层牌（double-layer plate）**：
上下两行的黄牌，需先切分再分别识别。
_Avoid_: 双行牌
