# --- 临时/试验产物（不入库）---
_scratch/
.scratch/
_hw_docs/
_hw_nodes.csv
_hw_tree.json
_fetch.ps1

# --- 工具运行痕迹 ---
.mimosa/
.playwright-mcp/

# --- Agent 会话状态（保留在磁盘，但不进公开仓库）---
# 说明：这不是缓存，是项目数据；但它是**工具内部状态**，
# 对读者没有价值，且会随每次会话变动。故排除在版本控制之外，不删除。
.workbuddy/
.pytest_cache/

# --- 构建产物 ---
build/
.cxx/
oh_modules/
node_modules/
*.hap
*.har

# --- 大体积模型（按需重新转换，见 docs/adr/0008）---
*.ms
*.om
*.omc
*.onnx
*.ncnn.bin

# --- 系统 ---
Thumbs.db
desktop.ini
.DS_Store
